version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Prueba
          command: echo "Hola CircleCI"

workflows:
  version: 2
  build:
    jobs:
      - build
